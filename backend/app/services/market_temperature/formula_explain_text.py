"""大盘温度「?」弹层：三因子详细计算与设计说明（与 formula.md、formula_engine.py v1.0.0 一致）。"""

from __future__ import annotations

SCORE_PIPELINE_TITLE = "原始值如何变成 0～100 分，再合成总分"

SCORE_PIPELINE_BODY = """【步骤 A：算「原始值」】每个交易日、每个因子先在四只指数上各算一组成分，再对四指数取**算术平均**，得到一个全市场层面的 trend_raw / liq_raw / risk_raw（与代码里对四指数 trend_raw 求 mean 等价，也等价于各给 25% 权重再求和）。

【步骤 B：滚动分位映射】对每个因子，维护「从首日到当前日」该因子每日 raw 构成的序列。映射时只取该序列末尾至多 756 个交易日（约 3 年）作为对比窗口。设窗口长度为 L，当天原始值为 x：

• 数窗口里有多少个值**严格小于** x，记为 less；有多少个值**等于** x，记为 eq。
• 分位数 p = (less + 0.5 × eq) / L。（相等值按一半计入排序位置，避免并列时跳跃。）

【步骤 C：成分得分】
• 趋势、量能（越高越热）：该因子得分 = p × 100。
• 风险（越高越冷，反向）：该因子得分 = (1 − p) × 100。

【步骤 D：总温度】总温度 = 0.4 × 趋势得分 + 0.3 × 量能得分 + 0.3 × 风险得分，结果保留两位小数，理论范围 [0, 100]。

若自算力起始日以来尚不足 756 个已有 raw 的日期，则用当前已有最长历史作窗口（预热阶段可比性略弱，与 formula 文档 warmup 语义一致）。"""

# 顶上「description」仅用一句话说明本因子管什么，不出现公式，避免与「计算方式」重复
TAGLINES = {
    "趋势": "只用价格和均线结构，衡量指数在短中期均线意义上的偏高/偏低（不含成交量、不含波动率）。",
    "量能": "只用成交额与成交量相对其自身 20 日均值的放大倍数，衡量资金参与度（不含均线多空、不含波动率）。",
    "风险": "用近一段日收益的离散程度与当日振幅衡量扰动大小；raw 越大表示越「险」，映射时反向压分，使总温度偏冷。",
}

FACTOR_EXPLAIN: list[dict[str, str]] = [
    {
        "factor_name": "趋势",
        "summary": TAGLINES["趋势"],
        "calculation_detail": """一、指数范围（与实现一致）
对以下四只指数**分别**计算，再对四者的 trend_raw_i 取算术平均得到当日 trend_raw（四者等价权重）。
• 000001.SH 上证指数
• 399001.SZ 深证成指
• 399905.SZ 中证500（Tushare 代码口径以行情表为准）
• 399852.SZ 中证1000

二、均线 MA20、MA60 的定义（与 _rolling_mean 一致）
对每个指数，将历史行情按交易日期升序排成收盘价序列 close[]，当日下标为 i（从 0 起）：
• MA20(i) = 收盘价序列上「以 i 为右端点」的最近若干日的算术平均。若 i+1 < 20，则取 close[0]…close[i] 全部参与平均（**未满 20 日时不用未来数据凑足 20**）；若 i+1 ≥ 20，则取 close[i−19]…close[i] 共 20 个收盘。
• MA60(i) 同理，窗口长度改为 60，不足 60 时同样只用已有历史。

三、单指数、当日的中间量与 trend_raw_i
记当日收盘 C = close[i]，MA20 = MA20(i)，MA60 = MA60(i)（若分母为 0 则该小项按 0 处理）：

1）trend_a = C ÷ MA20 − 1  
含义：收盘相对 **20 日均线** 的偏离率；>0 在短均线上方，<0 在下方。

2）trend_b = MA20 ÷ MA60 − 1  
含义：**20 日均线** 相对 **60 日均线** 的相对位置；>0 表示短均在中长均之上（多头排列倾向）。

3）trend_raw_i = 0.6 × trend_a + 0.4 × trend_b  
对「价格相对 MA20」给 **60%** 权重，对「MA20 相对 MA60 结构」给 **40%** 权重。

四、合成与映射
• 设四指数算出的单指数 raw 为 trend_raw_i（i=1..4），则  
  trend_raw = (trend_raw_1 + trend_raw_2 + trend_raw_3 + trend_raw_4) ÷ 4
• 再用上文「滚动分位」把 trend_raw 映射为 0～100 的 **趋势得分**（正向因子，raw 越高通常对应历史分位 p 越高，得分越高）。""",
        "design_rationale": """为什么一定是「均线」而不是「当日涨跌幅」：单日涨跌对噪声敏感；均线代表近 20/60 个交易日的平均成本与共识位置，更贴近「这一段行情是在均线之上运行还是之下修复」，和直觉上的「冷温热」更一致。

MA20 与 MA60 分工：只看 MA20 会忽略「中期均线是否仍压制」；trend_b 把短周期与中周期成本线关系纳入，避免「跌深反弹一天」就被误读为趋势反转。

权重 0.6 / 0.4：更信任**当前价相对 MA20** 的部位（多数用户先看离短均远近），再用均线排列做「第二层校验」，避免结构一面倒时分数漂移过快。

四指数等权：仅以上证大盘为代表的宽基往往失真于小盘；叠加深成指、500、1000 后，合成 raw 对「全市场宽度」更敏感，单指数失真时会被其它指数拉回一部分。""",
    },
    {
        "factor_name": "量能",
        "summary": TAGLINES["量能"],
        "calculation_detail": """一、指数范围
与趋势因子相同四只指数，各算 liq_raw_i，再对四者取算术平均得到 liq_raw。

二、20 日均额、均量（与 _rolling_mean 同一套窗口规则）
对每个指数，在升序行情序列下标 i 当日：
• MA20(amount)(i)：成交额 amount 序列上以 i 为右端点的「最近 20 日」算术平均；不足 20 日时仅用完历史（与趋势 MA 规则相同）。
• MA20(vol)(i)：成交量 vol 序列同理。

三、单指数、当日的 liq_raw_i
记当日成交额 A = amount[i]，当日成交量 V = vol[i]，分母非零时：

1）liq_a = A ÷ MA20(amount)(i)  
即当日成交额相对近一段均额的倍数；>1 表示比近期更「放量」。

2）liq_b = V ÷ MA20(vol)(i)  
同理为成交量相对近一段均量的倍数。

3）liq_raw_i = 0.7 × liq_a + 0.3 × liq_b  
**成交额权重 70%**，成交量 **30%**（与 A 股分析里「额比量更稳」更一致）。

4）若当日成交量字段缺失或均量为 0 使 liq_b 不可用，则实现上退化为 **liq_raw_i = liq_a**（只用成交额比）。

四、合成与映射
• liq_raw = (四指数 liq_raw_i) 的算术平均。
• 再对 liq_raw 做滚动分位映射得 **量能得分**（正向因子）。""",
        "design_rationale": """为什么单独拉一维「量」：同样一根阳线，放量与缩量背后的「资金共识强度」不同；温度希望既反映价位结构（趋势）、又反映参与深度（量），故与涨跌幅/均线解耦。

为何与自身 20 日均比，而不是固定绝对额：不同纪元的成交额绝对水平不可比；用「相对自身近一个月均值」可把「比最近是否更活跃」抽出来，再放进 3 年分位里做横向校准。

为何 risk 不放进本因子：波动与跳空属于不确定性，在风险因子用**反向**处理；量能因子只回答「有多少人真金白银在交易」，不直接惩罚波动。

0.7/0.3：额对大小盘、高价股结构更稳健；量作辅证，减少单一字段异常时对 raw 的支配。""",
    },
    {
        "factor_name": "风险",
        "summary": TAGLINES["风险"],
        "calculation_detail": """一、指数范围
仍为上述四只指数。各算 risk_raw_i，再算术平均得 risk_raw。

二、日收益序列（用于波动）
对每个指数，升序行情下标 i>0 且前日收盘 >0 时，追加一日收益：
• r_i = close[i] ÷ close[i−1] − 1
首日或无昨收时不产生新的 r（与实现一致）。

三、vol20：已实现中取「最近至多 20 个已产生的 r」算标准差（不是再往前取固定日历 20 日如果中间缺日，以代码窗口为准）。
设当前窗口内收益序列为 r_1…r_n（n 可为 1～20，但 n<2 时实现上标准差为 0）：
• 均值 μ = (r_1 + … + r_n) ÷ n
• 波动率 vol20 = sqrt( Σ_{k=1..n} (r_k − μ)² ÷ n )
即**总体标准差**（除以 n，与 formula_engine._std 一致；不是除以 n−1 的样本无偏版本）。n 越大、收益越分散，vol20 越大。

四、当日振幅 amp
• amp = (high − low) ÷ close（当日最高、最低、收盘；收盘为 0 时避免除零）
无量纲，便于不同点位指数间比较「日内拉锯」强度。

五、单指数 risk_raw_i 与合成
• risk_raw_i = 0.7 × vol20 + 0.3 × amp  
更重视 **一段时期内的收益波动**（70%），**当日极端震荡**占 30%。

• risk_raw = (四指数 risk_raw_i) 的算术平均。

六、反向映射
对 risk_raw 在滚动窗口内算分位数 p 后：
• 风险得分 = (1 − p) × 100
即：相对历史**越危险**（p 大），**得分越低**，从而在总温度里向下拉。""",
        "design_rationale": """「温度」在此偏向「参与舒适度 + 环境是否躁动」，不是鼓励裸追波动。波动大、日内振幅大 → 环境更难押注 → 即便收涨，也可能仍给低分，这与「反弹日总分仍可能低于暴跌日」等现象在机制上可兼容（另两个因子也在同时作用）。

vol20 与 amp 并列：只有 vol20 会对「单日怪 K」钝感；只有 amp 会忽略「连续多日小碎步累积的乱」。0.7/0.3 让「环境」主导、「当天是否极端」修正。

为何必须与趋势因子分开计价：趋势描述「位置」，风险描述「稳不稳」；暴涨暴跌往往抬风险 raw，反向映射后压低总分，避免用户把「剧烈震荡」误读为「健康高温」。

四指数等权：若只有大盘波动低、小盘波动高，平均后仍能体现广义市场不稳。""",
    },
]
