# 实现计划：[功能名称]

**分支**: `[###-feature-name]` | **日期**: [日期] | **规格**: [链接到 spec.md]  
**输入**: 功能规格来自 `/specs/[###-feature-name]/spec.md`

**说明**: 本模板由 `/speckit.plan` 命令填写。全文须使用**中文**，且达到**可直接按方案实现**的粒度。

## 概要

[从规格与调研中提炼：主要需求 + 技术路线简述，用中文]

## 技术背景

<!-- 用中文填写；未知项标「待澄清」，在 Phase 0 调研中解决 -->

- **语言/版本**: [如 Python 3.12、TypeScript 5.x 或 待澄清]
- **主要依赖**: [如 FastAPI、Vue 3、MySQL 或 待澄清]
- **存储**: [如 MySQL、PostgreSQL、文件 或 不适用]
- **测试**: [如 pytest、vitest 或 待澄清]
- **目标平台**: [如 Linux 服务器、现代浏览器 或 待澄清]
- **项目类型**: [如 Web 应用（前后端）、CLI、库 或 待澄清]
- **性能目标**: [如 选股首屏 <3s、QPS 上限 或 待澄清]
- **约束**: [如 数据源限流、内存上限 或 待澄清]
- **规模/范围**: [如 全市场约 5000 只标的、单用户 或 待澄清]

## 章程检查

*门禁：Phase 0 调研前须通过；Phase 1 设计后复检。*

[根据 constitution 文件填写；若未核定则注明「未核定，无强制门禁」]

## 关键设计详述

<!-- 必填。此处写清可直接落地的设计，禁止概要式一句话。若涉及定时任务，必须完整填写「定时任务与部署设计」。 -->

### 数据流与接口职责

[写清：数据从哪来到哪去、经过哪些层；后端哪些接口、前端哪些页面/组件；关键请求路径、主要参数、响应结构、错误约定。]

### 定时任务与部署设计

**（若本功能不涉及定时任务，可写「本功能不涉及定时任务」，并删去下列子项。若涉及，必须逐项填写。）**

- **使用的组件**: [如 APScheduler / 系统 cron / Celery，以及该组件在项目中的位置，如 backend/app/core/scheduler.py]
- **注册方式**: [在应用何处、何时注册任务，如 FastAPI lifespan 的 startup 中调用 scheduler.add_job(...)]
- **调度策略**: [具体时间或 cron 表达式，如「每日 17:00」或 `0 17 * * *`]
- **部署时是否执行一次**: [是/否]。若「是」：[触发方式：应用启动后延迟 N 秒执行一次 / 或 部署脚本中单独调用一次拉数接口或命令]
- **手动触发方式**: [必须提供至少一种]
  - [ ] HTTP 接口：[方法 与 路径，如 POST /api/admin/stock-sync]，鉴权方式：[如 仅本地、或 管理员 token]
  - [ ] 或 管理命令：[如 `python -m app.tasks.sync_stock`]
  - [ ] 或 脚本：[如 `scripts/sync-stock.sh`]
- **失败与重试**: [失败是否重试、最多重试次数、重试间隔；是否写日志、是否需告警]
- **日志与可观测**: [关键步骤打什么日志、日志级别；是否有执行耗时或条数统计]

### 其他关键设计

[若还有对实现有直接影响的决策，在此写清：如鉴权方式、分页上限、外部接口限流下的批处理策略等。]

## 项目结构

### 本功能文档

```text
specs/[###-feature]/
├── plan.md              # 本文件
├── research.md          # Phase 0 调研结论
├── data-model.md        # Phase 1 数据模型
├── quickstart.md        # Phase 1 本地运行与验证
├── contracts/           # Phase 1 接口契约
└── tasks.md             # Phase 2 由 /speckit.tasks 生成
```

### 源码结构（仓库根目录）

[根据实际项目填写具体目录树，删除未用选项；用中文注释关键目录职责。]

```text
backend/
├── app/
│   ├── api/             # 路由
│   ├── core/            # 配置、调度器等
│   ├── models/
│   ├── services/
│   └── ...
└── tests/

frontend/
├── src/
│   ├── views/           # 页面
│   ├── components/
│   └── api/
└── tests/
```

**结构说明**: [简短说明为何采用该结构，以及与本功能对应的目录/文件。]

## 复杂度与例外

> **仅当章程检查存在需要说明的违反时填写**

| 违反项 | 为何需要 | 为何不采用更简单方案 |
|--------|----------|----------------------|
| [例：引入新中间件] | [当前需求] | [为何现有方案不足] |
