# 调研结论：历史高低价（Phase 0）

**关联规格**：[spec.md](./spec.md)  
**日期**：2026-03-28

## 1. 增量任务与日线同步的先后关系

- **决策（2026-03-29 修订）**：**不再**使用 18:00 独立 Cron；在 `stock_daily_bar_sync_service` 每次日线 upsert 后调用 `apply_cum_extrema_after_daily_upsert`，与日行情**同事务路径**一致，避免「日线未写完就算极值」的窗口期。
- **理由（历史）**：曾计划 17:00 同步后 18:00 跑增量；现改为写入即递推，语义更简单、延迟更低。
## 2. 「增量」计算口径（实现策略）

- **决策（2026-03-29 修订）**：累计极值写在 `stock_daily_bar.cum_hist_*`。**纯尾部追加**：用上一交易日行的累计值与当日 `high`/`low` 递推（O(1)）。**存在更晚 `trade_date` 或累计链断裂**：对该 `stock_code` 按时间序整段重算，与全历史递推一致。
- **理由**：日常成本低；中间改数仍正确。**全量 CLI** `run_full_recompute` 用于迁移后首填与批量纠偏。
- **备选**：每晚全市场逐股整段重算——可作为运维兜底，非默认路径。

## 3. 全量重算入口形态

- **决策**：**不提供** HTTP；在仓库内提供 **本机可执行脚本**（如 `backend/scripts/recompute_hist_extrema_full.py`），内建 `SessionLocal` + 服务层 `run_full_recompute`，支持从项目根或 `backend` 目录以 `python scripts/...` 调用。
- **理由**：与 Clarifications（用户明确本机 CLI、不对外暴露）一致；与现有「无统一 CLI 模块、以脚本为主」的仓库习惯兼容。
- **备选**：`manage.py` 子命令——需新建基础设施，非必要。

## 4. 复权与数据源

- **决策**：极值直接取自 `stock_daily_bar.high` / `low` 已存数值，**不**在任务内另行做复权转换；与项目当前日线表语义一致。
- **理由**：规格假设「日行情模型一致」；若未来日线改为多复权版本，应通过日线表/schema 演进单独规格，而非本任务内猜测。
- **备选**：单独存不复权/前复权两套极值——超出当前规格范围。

## 5. 与 003 列表 API 的契约关系

- **决策**：列表仍使用 **`GET /api/stock/basic`**，在 `items[]` 中**扩展字段** `hist_high`、`hist_low`、`hist_extrema_computed_at`；不在 013 新增独立 REST 资源路径。
- **理由（2026-03-29）**：展示字段仍从列表接口带出；数值源为**最新日线行**的 `cum_hist_*`（左连），与 003 页面一屏展示一致。
- **备选**：单独 `GET /stock/basic/extrema`——多一次请求，不必要。
